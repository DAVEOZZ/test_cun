TEST_CUN

Instrucciones:

 python -m venv .venv
 
 cd .venv
 
 Scripts\activate
 
 pip install -r requirements.txt
 
 cd ..
 
 uvicorn presentation.main2:app --reload
 
Or

 Docker :
 
 docker-compose up --build
 
